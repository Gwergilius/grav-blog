{% if page.header.series %}
{% set series_name = page.header.series.name %}
{% set current_part = page.header.series.part %}
{% set total_parts = page.header.series.total %}

<div class="series-info">
    <h4>📚 Series: {{ series_name }}</h4>
    <p>Part {{ current_part }} of {{ total_parts }}</p>
    
    {% if current_part > 1 %}
        <!-- Previous part link logic -->
    {% endif %}
    
    {% if current_part < total_parts %}
        <!-- Next part link logic -->
    {% endif %}
</div>
{% endif %}